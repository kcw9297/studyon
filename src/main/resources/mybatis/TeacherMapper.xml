<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="studyon.app.layer.domain.teacher.mapper.TeacherMapper">
    <!-- ✅ 강사 목록 조회 (검색 + 페이징) -->
    <select id="findBySearch" parameterType="map" resultType="studyon.app.layer.domain.teacher.TeacherDTO$Read">
        SELECT
        t.teacher_id AS teacherId,
        m.member_id AS memberId,
        m.nickname AS nickname,
        m.email AS email,
        m.role AS role,
        m.is_active AS active,
        m.cdate AS createdAt,
        m.last_login_at AS lastLoginAt,
        t.subject AS subject,
        t.total_students AS totalStudents,
        t.total_review AS totalReview,
        t.average_rating AS averageRating,
        t.description AS description
        FROM teacher t
        JOIN member m ON t.member_id = m.member_id
        WHERE 1=1
        AND m.role = 'ROLE_TEACHER'
        <if test="rq.keyword neq null and rq.keyword neq ''">
            <choose>
                <when test="rq.filter eq 'email'">
                    AND m.email LIKE CONCAT('%', #{rq.keyword}, '%')
                </when>
                <when test="rq.filter eq 'nickname'">
                    AND m.nickname LIKE CONCAT('%', #{rq.keyword}, '%')
                </when>

                <otherwise>
                    AND (
                        m.email LIKE CONCAT('%', #{rq.keyword}, '%')
                        OR m.nickname LIKE CONCAT('%', #{rq.keyword}, '%')
                    )
                </otherwise>
            </choose>
        </if>
        <if test="rq.subject != null">
            AND t.subject = '${rq.subject.name()}'
        </if>
        ORDER BY t.teacher_id DESC
        LIMIT #{prq.size} OFFSET #{prq.startPage}
    </select>

    <!-- ✅ 카운트 쿼리 -->
    <select id="countBySearch" parameterType="map" resultType="int">
        SELECT COUNT(*)
        FROM teacher t
        JOIN member m ON t.member_id = m.member_id
        WHERE 1=1
        AND m.role = 'ROLE_TEACHER'
        <if test="rq.keyword neq null and rq.keyword neq ''">
            <choose>
                <when test="rq.filter eq 'email'">
                    AND m.email LIKE CONCAT('%', #{rq.keyword}, '%')
                </when>
                <when test="rq.filter eq 'nickname'">
                    AND m.nickname LIKE CONCAT('%', #{rq.keyword}, '%')
                </when>
            </choose>
        </if>
        <if test="rq.subject != null">
            AND t.subject = '${rq.subject.name()}'
        </if>
    </select>

    <!-- ✅ 전체 목록 (페이징X) -->
    <select id="findAll" resultType="studyon.app.layer.domain.teacher.TeacherDTO$Read">
        SELECT
            t.teacher_id AS teacherId,
            m.member_id AS memberId,
            m.nickname AS nickname,
            m.role AS role,
            m.is_active AS active,
            m.cdate AS createdAt,
            m.last_login_at AS lastLoginAt,
            t.subject AS subject,
            t.total_students AS totalStudents,
            t.total_review AS totalReview,
            t.average_rating AS averageRating,
            t.description AS description
        FROM teacher t
                 JOIN member m ON t.member_id = m.member_id
        WHERE 1=1
          AND m.role = 'ROLE_TEACHER'

        ORDER BY t.teacher_id DESC
    </select>

</mapper>